# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
ENV/
env/
.env

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Note: LaTeX sources and outputs are tracked and should not be ignored.
# Removed LaTeX-specific ignore patterns so .tex and generated PDFs can be committed.

# Temporary files
*.tmp
*.bak
*.swp
.~*

# OS files
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Credentials and secrets
.env.local
.env.*.local
secrets.json

# Large binary files (keep source, not compiled)
# PDFs in outputs/compiled are generated - can be regenerated
# Keep curriculum PDFs as they're sources

# Optional: Keep compiled PDFs if desired, or regenerate from source
# Uncomment if you want to exclude:
# outputs/compiled/*.pdf
# outputs/compiled/figures/
# outputs/compiled/images/

# Cache
.cache/
*.cache

# Lock files (optional - include if you want reproducible versions)
# poetry.lock
# Pipfile.lock
